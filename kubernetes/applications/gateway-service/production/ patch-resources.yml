apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
  labels:
    app: gateway-service
spec:
  template:
    spec:
      containers:
        - name: gateway-service
          resources:
            requests:
              cpu: 0.1
              memory: 756Mi
            limits:
              cpu: 2
              memory: 756Mi
